# Changes 2026-04-17

- Reworked desktop UI closer to provided mockup: compact left sidebar, server workspace, animated connection stage, stats screen, logs screen.
- Added black / orange / white accent styling and settings controls for theme, accent preset, and animations.
- Updated import modal and subscription modal labels to match the requested flow.
- Replaced app icons with the provided logo and added the logo to the interface.
- Updated sing-box runtime DNS config to the new DNS server format.
- Hid spawned sing-box console windows on Windows via hidden process creation flags.
- Stripped ANSI color codes from sing-box validation errors for cleaner error messages.
- Frontend production build was verified with `npm run build`.
